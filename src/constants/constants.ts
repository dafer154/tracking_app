export const IP = 'http://localhost:4000';

export const API = `${IP}/api`;

export const Roles = {
    GETALL: `${API}/roles`
}

export const Login = {
    AUTH: `${API}/login`,
    LOGOUT: `${API}/logout`
}
